import { LoaderContext } from 'webpack';
import { Options } from './options';
export declare const raw = true;
export default function repackAssetsLoader(this: LoaderContext<Options>): Promise<void>;
